name: 'anthropic-release-notes'
description: 'Generate release notes with Anthropic Claude from commits since the last release'
author: 'Copilot Breakpoint Debugger'
inputs:
  anthropic-api-key:
    description: 'Anthropic API Key'
    required: true
  language:
    description: 'Language for release notes (default en)'
    required: false
    default: 'en'
  model:
    description: 'Anthropic model name. Leave blank to auto-select newest model via /v1/models.'
    required: false
    default: ''
  token:
    description: 'GitHub token for API calls'
    required: true
  version:
    description: 'Release version (tag) to create'
    required: true
runs:
  using: 'node20'
  main: 'index.js'
